package com.atguigu.scala.p4

object Scala03_Logic_while {
    def main(args: Array[String]): Unit = {
       //Todo 逻辑 - 循环 - while

        //while和for的区别：while有外部的参数的值决定，而for有自己的条件判断式来决定

        var flag = true
        while(flag){
            println("xxxx")
            flag = false
        }
    }
}
